<div style="margin-top: 20px;">
	<% if current_school_user.present? && current_school_user.is_a?(Student) %>
		<%= link_to 'List Classmates', classmates_school_course_batch_student_path(current_school_user.school_id, current_school_user.course_id, current_school_user.batch_id, current_school_user.id) %>
	<% else %>
		<%= link_to 'List Schools', schools_path %>
	<% end %>
</div>

